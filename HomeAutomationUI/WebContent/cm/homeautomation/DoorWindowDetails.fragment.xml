<core:FragmentDefinition xmlns="sap.m"
	xmlns:core="sap.ui.core">
	<Dialog title="Fenster und Türen" afterClose="afterDoorWindowDialogClose"
		stretch="true" horizontalScrolling="false" verticalScrolling="true"
		contentHeight="100%">
		<content>
			<Panel expandable="false" expanded="true" width="auto"
				class="sapUiResponsiveMargin" id="remoteControl">
				<content>

					<Table
						items="{
			path: 'doorWindow>/',
			sorter: {
				path: 'doorWindow>device/name'
			}
		}"
						title="Devices" selectionMode="MultiToggle">
						<columns>
							<Column>
								<Text text="Name" />
							</Column>
							<Column>
								<Text text="Zustand" />
							</Column>
						</columns>

						<items>
							<ColumnListItem>
								<cells>
									<Text text="{doorWindow>device/name}" />
									<Text text="{=${doorWindow>state} === 0 ? 'geschlossen': 'offen' }" />

								</cells>
							</ColumnListItem>
						</items>
					</Table>
				</content>
			</Panel>


		</content>
		<beginButton>


		</beginButton>
		<endButton>
			<Button text="Schliessen" press="doorWindowDialogClose" />
		</endButton>

	</Dialog>
</core:FragmentDefinition>
